import { QUIZ_REGISTRY, getQuizQuestions } from './quiz/quizRegistry';

/**
 * DEPRECATED: Use getQuizQuestions from ./quiz/quizRegistry instead.
 * Keeping this for backward compatibility with existing screens.
 */
export const ALL_QUIZ_QUESTIONS = QUIZ_REGISTRY;
